# 🚀 راهنمای کامل اجرای وب‌سایت

این راهنما **قدم به قدم** بهت میگه چطور وب‌سایت رو اجرا کنی.

---

## ⚡ سریع‌ترین روش (3 دقیقه)

### قدم 1: برو توی پوشه webapp

```bash
cd webapp
```

### قدم 2: اجرا کن!

**Linux/Mac:**
```bash
./START_WEBAPP.sh
```

**Windows:**
```bash
START_WEBAPP.bat
```

**یا با Python مستقیم:**
```bash
pip install -r requirements.txt
python app.py
```

### قدم 3: باز کن!

مرورگر رو باز کن و برو به:
```
http://localhost:5000
```

**تمام! 🎉**

---

## 📸 چه چیزی میبینی؟

### صفحه اصلی
```
┌─────────────────────────────────────────┐
│  🔍 تشخیص پروپاگاندا در میم‌ها          │
│                                         │
│  ┌───────────────────────────────┐    │
│  │         📤                    │    │
│  │  میم خود را اینجا بکشید      │    │
│  │  یا کلیک کنید                │    │
│  │                               │    │
│  │  JPG, PNG, GIF (max 16MB)    │    │
│  └───────────────────────────────┘    │
│                                         │
│      [🔬 تحلیل میم]                    │
│                                         │
│  📊 این سیستم چطور کار می‌کنه؟         │
│  • مدل هوش مصنوعی AdaBoost            │
│  • Knowledge Graph                     │
│  • تشخیص 22 تکنیک پروپاگاندا           │
└─────────────────────────────────────────┘
```

### بعد از آپلود
```
در حال تحلیل میم... ⏳
```

### نتایج
```
┌─────────────────────────────────────────┐
│  [تصویر میم شما]                        │
│                                         │
│  ✅ تکنیک‌های پروپاگاندا شناسایی شده   │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ ✓ Loaded Language                │ │
│  │ ████████████░░░░░ 75%             │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ ✓ Appeal to Emotions             │ │
│  │ ██████████░░░░░░░ 68%             │ │
│  └───────────────────────────────────┘ │
│                                         │
│  📈 امتیازات تمام تکنیک‌ها              │
│  ○ Bandwagon          45%             │
│  ○ Doubt              38%             │
│  ○ ...                                │
│                                         │
│      [🔄 آپلود میم جدید]               │
└─────────────────────────────────────────┘
```

---

## 🎯 فعلاً چی داری؟

### ✅ چیزهایی که آماده‌اند:

1. **رابط کاربری زیبا** - فارسی/انگلیسی
2. **آپلود فایل** - Drag & Drop
3. **نمایش نتایج** - نمودار و امتیاز
4. **Knowledge Graph** - بهبود دقت
5. **طراحی Responsive** - موبایل و دسکتاپ

### ⚠️ چیزی که باید اضافه کنی:

**مدل AdaBoost خودت!**

فعلاً سیستم با پیش‌بینی‌های **تصادفی** کار می‌کنه (برای نمایش).
برای استفاده واقعی، باید مدلت رو وصل کنی.

---

## 🔧 اتصال مدل واقعی (10-30 دقیقه)

### راه کوتاه:

1. **ذخیره مدل:**
   ```python
   # در نوت‌بوک ali_write_adaboost_ensemble.ipynb
   torch.save(model.state_dict(), '../saved_models/model.pth')
   ```

2. **ساخت بارگذارنده:**
   ```bash
   # ساخت فایل جدید
   nano webapp/model_loader.py
   ```

   ```python
   # محتوای فایل
   import torch

   class ModelLoader:
       def __init__(self):
           self.model = torch.load('../saved_models/model.pth')

       def predict(self, image_path):
           # کد پیش‌بینی از نوت‌بوک
           return predictions
   ```

3. **ویرایش app.py:**
   ```python
   # پیدا کن:
   model = None  # Placeholder

   # جایگزین کن با:
   from model_loader import ModelLoader
   model = ModelLoader()
   ```

**راهنمای کامل:** `MODEL_INTEGRATION_GUIDE.md`

---

## 📝 تست سریع

### بدون مدل واقعی:

```bash
cd webapp
python app.py
```

باز کن: `http://localhost:5000`

یک تصویر آپلود کن → نتایج **تصادفی** میبینی (برای نمایش)

### با مدل واقعی:

بعد از اتصال مدل، نتایج **واقعی** میبینی! 🎯

---

## 🎨 شخصی‌سازی

### تغییر رنگ‌ها

فایل `templates/index.html` → خط 17:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

رنگ دلخواه:
```css
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%); /* قرمز-فیروزه‌ای */
```

### تغییر عنوان

همون فایل → خط 206:
```html
<h1>🔍 تشخیص پروپاگاندا در میم‌ها</h1>
```

### اضافه کردن لوگو

```html
<img src="/static/logo.png" alt="Logo">
```

---

## 🐛 حل مشکلات رایج

### مشکل 1: سایت باز نمیشه

**علت:** پورت 5000 اشغاله

**حل:**
```python
# در app.py آخرین خط:
app.run(debug=True, host='0.0.0.0', port=8000)  # پورت دیگه
```

باز کن: `http://localhost:8000`

---

### مشکل 2: خطای "ModuleNotFoundError"

**علت:** بسته‌ها نصب نشدن

**حل:**
```bash
pip install -r requirements.txt
```

یا یکی یکی:
```bash
pip install Flask Pillow numpy networkx
```

---

### مشکل 3: فایل آپلود نمیشه

**چک کن:**
- سایز فایل کمتر از 16MB؟
- فرمت JPG/PNG/GIF؟
- Console مرورگر چی میگه؟ (F12 → Console)

---

### مشکل 4: نتایج تصادفی‌ن!

**طبیعیه!** مدل واقعی رو هنوز وصل نکردی.

**حل:** `MODEL_INTEGRATION_GUIDE.md` رو بخون

---

## 💡 نکات مهم

### ✅ کارهایی که میشه انجام داد:

1. **فعلاً رابط کاربری رو ببین**
2. **تست کن که آپلود کار می‌کنه**
3. **ببین نتایج چطور نمایش داده میشه**
4. **بعد مدل رو وصل کن**

### ❌ کارهایی که نباید انجام داد:

1. **منتظر مدل واقعی نباش** - اول سایت رو ببین
2. **نگران نتایج تصادفی نباش** - برای نمایش هست
3. **فایل‌ها رو تغییر ندی** - تا موقعی که نمیدونی چیکار می‌کنی

---

## 📊 مقایسه Before/After

### الان (بدون مدل):
- ✅ سایت کار می‌کنه
- ✅ آپلود کار می‌کنه
- ✅ نمایش نتایج کار می‌کنه
- ⚠️ نتایج تصادفی (برای نمایش)

### بعد از اتصال مدل:
- ✅ سایت کار می‌کنه
- ✅ آپلود کار می‌کنه
- ✅ نمایش نتایج کار می‌کنه
- ✅ **نتایج واقعی از مدل شما!** 🎯

---

## 📂 ساختار کامل

```
meme_propaganda_detection/
│
├── webapp/                          ← اینجایی!
│   ├── app.py                       ← سرور Flask
│   ├── templates/
│   │   └── index.html               ← رابط کاربری
│   ├── uploads/                     ← فایل‌های موقت
│   ├── requirements.txt             ← بسته‌ها
│   ├── START_WEBAPP.sh              ← اجرا (Linux/Mac)
│   ├── START_WEBAPP.bat             ← اجرا (Windows)
│   ├── MODEL_INTEGRATION_GUIDE.md   ← راهنمای اتصال مدل
│   ├── README.md                    ← مستندات
│   └── HOW_TO_RUN.md               ← این فایل
│
├── propaganda_techniques.json       ← تعاریف تکنیک‌ها
├── three_stream_architecture.py     ← Knowledge Graph
├── prerequisite_checker.py          ← چک‌کننده پیش‌نیازها
└── knowledge_graph.py               ← گراف دانش
```

---

## ✅ چک‌لیست

قبل از اجرا:
- [ ] `cd webapp` کردم
- [ ] `pip install -r requirements.txt` اجرا کردم
- [ ] `python app.py` رو زدم

بعد از اجرا:
- [ ] مرورگر باز شد
- [ ] صفحه اصلی رو دیدم
- [ ] یک تصویر آپلود کردم
- [ ] نتایج نمایش داده شد

برای استفاده واقعی:
- [ ] مدل رو ذخیره کردم
- [ ] `model_loader.py` ساختم
- [ ] `app.py` رو ویرایش کردم
- [ ] با میم واقعی تست کردم

---

## 🆘 کمک بیشتر

| چیکار می‌خوای بکنی؟ | کجا رو ببین؟ |
|---------------------|--------------|
| اجرای سریع سایت | همین فایل! |
| اتصال مدل AdaBoost | `MODEL_INTEGRATION_GUIDE.md` |
| تنظیمات و شخصی‌سازی | `README.md` |
| مستندات کامل | `../README_KG_ARCHITECTURE.md` |
| نوت‌بوک آموزشی | `../START_HERE_Integration.ipynb` |

---

## 🎯 خلاصه

```bash
# 1. برو توی پوشه
cd webapp

# 2. اجرا کن
./START_WEBAPP.sh  # یا START_WEBAPP.bat

# 3. باز کن
http://localhost:5000

# 4. لذت ببر! 🎉
```

**بعداً مدل رو وصل کن:** `MODEL_INTEGRATION_GUIDE.md`

---

**موفق باشی! 🚀**

اگه سوالی داشتی، فایل‌های راهنما رو بخون یا من رو صدا کن! 😊
